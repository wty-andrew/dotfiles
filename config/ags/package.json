{
  "name": "ags-config",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon --ext ts,tsx,scss --signal SIGINT --exec 'ags run'",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "nodemon": "^3.1.7"
  }
}
